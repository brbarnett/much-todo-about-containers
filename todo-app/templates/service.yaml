apiVersion: v1
kind: Service
metadata:
  name: {{ template "todo-app.fullname" . }}
  labels:
    app: {{ template "todo-app.name" . }}
    chart: {{ template "todo-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "todo-app.name" . }}
    release: {{ .Release.Name }}
